# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: python-processor-service
  namespace: default
spec:
  selector:
    app: python-processor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
